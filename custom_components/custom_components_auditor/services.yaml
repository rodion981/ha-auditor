run_audit:
  fields:
    mode:
      required: false
      default: full
      selector:
        select:
          translation_key: audit_mode
          options:
            - daily
            - full
            - digest
    notify:
      required: false
      default: true
      selector:
        boolean:
